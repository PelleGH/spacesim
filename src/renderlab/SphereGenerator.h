#pragma once

// Compatibility include for old experimental sources. The shared generator now
// lives with engine geometry so both SpaceSim and RendererLab can use it.
#include "geometry/SphereGenerator.h"
