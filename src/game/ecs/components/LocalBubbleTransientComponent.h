#pragma once

namespace SpaceSim
{
    // Prototype/local encounter content with no persistent global anchor.
    // It is discarded when hyperdrive relocates the active gameplay bubble.
    struct LocalBubbleTransientComponent
    {
    };
}
